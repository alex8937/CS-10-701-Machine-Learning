function y = kernel_regression(XTrain,yTrain,XTest,bw,kernel_type)
    % XTrain: training data, size of nTrain * f
    % yTrain: training value, size of nTrain * 1
    % XTest: test data, size of nTest * f
    % bw: bandwidth
    % y: test value, size of nTest * 1

    nTest = size(XTest,1);
    y = zeros(nTest,1);
    
    %% begin
    
    %% end
    
end
